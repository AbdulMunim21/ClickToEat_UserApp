class ItemModel {
  constructor(id, title, imageUrl, price, sizes) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.price = price;
    this.sizes = sizes;
  }
}

export default ItemModel;
